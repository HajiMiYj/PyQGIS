# 二次开发手册

这份手册写给已经有一个 PyQGIS 独立应用、想从本项目取走某些 Action 的开发者。这里的 Action 指菜单、工具栏、右键菜单上的操作入口。本文以当前仓库代码和 QGIS 3.34.10、Windows OSGeo4W 环境为准。仓库根目录的 `README.md` 负责介绍整个项目；这里专门讲拆分、复制和接线。

先记住一件事：`QAction` 只负责显示文字、图标、快捷键和发出信号。一个能用的功能，通常还包括处理方法、地图工具或对话框、图层状态判断、Qt 资源和退出时的清理。复制一个 `.ui` 里的 `<action>`，相当于只复制了按钮。

## 先做一遍：把测量工具搬进另一个程序

下面做一个叫“测量工具移植课堂”的小程序。它不是本项目的 `QgisApp`，而是另一个独立的 `QMainWindow`。画布、图层树、菜单和工具栏都由它自己创建；测量距离、面积、角度、方位角则直接使用从本项目复制过去的四个 Python 模块。做完以后，读者可以拿自己的程序替换这个小窗口，按相同的位置接线。

范例入口在 [`examples/measure_port/main.py`](examples/measure_port/main.py)。第一次打开范例目录时，`src/app` 和 `src/ui` 只有 `__init__.py`。这是有意的：下面让读者亲手做一次“找文件、复制、运行”。整个例子只需要 OSGeo4W 的 PyQGIS 3.34.10，不需要系统 Python、pip 安装的 QGIS，也不需要编译 C++。

### 第 1 步：确认目录和启动方式

先在 PowerShell 里进入本仓库根目录。假如仓库路径与本机不同，改第一行即可：

```powershell
$repo = 'qgis_python的安装目录'
$demo = Join-Path $repo 'examples\measure_port'
Set-Location -LiteralPath $repo
Test-Path -LiteralPath 'C:\OSGeo4W\bin\python-qgis-ltr.bat'
```

最后一行必须得到 `True`。如果电脑安装的是另一套 OSGeo4W，后面的运行命令也要换成那套环境中的 QGIS Python 启动器。这个范例目录应有以下结构；测量文件由下一步补齐：

```text
examples/measure_port/
├─ main.py                         # 新项目自己的入口和主窗口
└─ src/
   ├─ __init__.py
   ├─ app/
   │  └─ __init__.py
   └─ ui/
      └─ __init__.py
```

### 第 2 步：复制功能本体，不复制整套 QGISApp

在刚才的 PowerShell 窗口执行：

```powershell
$appFiles = @(
    'qgsmeasuretool.py',
    'qgsmeasuredialog.py',
    'qgsmaptoolmeasureangle.py',
    'qgsmaptoolmeasurebearing.py'
)
foreach ($name in $appFiles) {
    Copy-Item -LiteralPath (Join-Path $repo "src\app\$name") `
              -Destination (Join-Path $demo 'src\app') -Force
}
Copy-Item -LiteralPath (Join-Path $repo 'src\ui\qgsmeasurebase.ui') `
          -Destination (Join-Path $demo 'src\ui') -Force
```

复制后，对照文件名检查：

```powershell
Get-ChildItem -LiteralPath (Join-Path $demo 'src\app') -Name
Get-ChildItem -LiteralPath (Join-Path $demo 'src\ui') -Name
```

这里不能只取 `qgsmeasuretool.py`。它在构造时导入同目录下的 `qgsmeasuredialog.py`；对话框再按 `Path(__file__).resolve().parents[1] / 'ui/qgsmeasurebase.ui'` 找界面文件。角度、方位角两个文件虽然只有很少几行，但它们是各自的类入口。保留 `src/app`、`src/ui` 和原文件名，就不用改这几处相对导入与路径计算。

**这个例子不需要 `pyuic` 或 `pyrcc`。** `QgsMeasureDialog` 运行时用 `uic.loadUi()` 读取 `qgsmeasurebase.ui`；这些测量 Action 的工具栏图标通过 `QgsApplication.getThemeIcon()` 从 OSGeo4W 的 QGIS 主题取得。既没有导入 `ui_qgsmeasurebase.py`，也没有使用 `:/images/...` 路径，所以不必生成这两类文件。以后复制别的功能时要重新检查，不能把这个结论套到所有 `.ui` 上。

### 第 3 步：先运行，看看完整结果

```powershell
& 'C:\OSGeo4W\bin\python-qgis-ltr.bat' (Join-Path $demo 'main.py')
```

窗口左侧是原生图层树，中间有一个内存中的 1000 米方形。菜单“测量”和同名工具栏有“平移、测量距离、测量面积、测量角度、测量方位角”。先点“测量距离”，沿方形底边依次点击两个角，再看测量窗口；再点“测量面积”，依次点击四个角，右键结束。具体数值会受工程椭球和测量设置影响，重点先确认工具能取点、对话框会更新、单位能切换。关闭再开这个范例时，示例图层会重新创建，它不是磁盘文件。

再依次检查：切到平移时原测量按钮取消选中；在“文件 → 添加矢量图层”里打开自己的 GeoPackage、Shapefile 或 GeoJSON，图层出现在左侧且画布缩放过去；关闭主窗口时测量窗口也关闭。范例的文件过滤器没有 GPX，遵守本项目排除 GPS 的范围。

若点击距离按钮后没有反应，先查上一步的五个文件是否放对，再查 **运行的是不是范例目录中的 `main.py`**。运行仓库根目录的 `main.py` 启动的是原项目，无法验证这次迁移。

### 第 4 步：读懂新项目入口的四处接线

打开范例的 [`main.py`](examples/measure_port/main.py)，顺着 `QgisApp.__init__()` 往下看。这里保留 `QgisApp`、`mMapCanvas`、`mActionMeasure` 等名称，是为了让源代码里的依赖一眼就能对上；这个类只属于范例项目，不会导入本仓库的大主窗口。

第一处是应用启动。`main()` 在创建窗口前设 `QgsApplication.setPrefixPath(...)`，创建带界面的 `QgsApplication`，依次调用 `initQgis()` 和 `QgsGui.editorWidgetRegistry().initEditors()`。这些是 PyQGIS 环境，不是某个测量按钮的代码。程序退出时先关窗口、清工程，再 `exitQgis()`。如果自己的项目已有可靠的启动和退出过程，就保留自己的入口，不要再创建第二个 `QgsApplication`。

第二处是工程、画布、图层树。`self.mProject = QgsProject.instance()` 之后，范例建立 `QgsMapCanvas`，设置 `theMapCanvas` 名称，并把工程、目标 CRS 交给画布。左侧 `QgsLayerTreeView` 使用 `QgsLayerTreeModel(self.mProject.layerTreeRoot(), self)`；`QgsLayerTreeMapCanvasBridge` 让树的可见性和顺序进入画布。测量工具依赖的是画布，图层树用于给学生一个正常的地图窗口。自己原有项目若已有这些对象，应当接已有实例，不能另外造一个不与画布相连的树。

第三处是真正移植测量工具的位置：

```python
self.mMapTools = {
    'pan': QgsMapToolPan(self.mMapCanvas),
    'measure': QgsMeasureTool(self.mMapCanvas, app=self),
    'measureArea': QgsMeasureTool(self.mMapCanvas, True, self),
    'measureAngle': QgsMapToolMeasureAngle(self.mMapCanvas),
    'measureBearing': QgsMapToolMeasureBearing(self.mMapCanvas),
}
```

构造顺序很重要：先有画布与工程，再有工具。四个工具被保存到 `self.mMapTools`，不是按钮触发时临时建一个局部对象。`QgsMeasureTool` 构造时会创建测量对话框；对话框和工具后续都要继续存活。角度与方位角类会经由画布找到顶层窗口，所以范例把画布设为主窗口的中央控件。

第四处是 Action 与工具切换。范例为每个工具创建一个稳定 `objectName` 的 `QAction`，给 Action 设置主题图标、放入 `QActionGroup`，调用 `tool.setAction(action)`，然后将 Action 放到菜单和工具栏。`triggered` 只负责调用 `mMapCanvas.setMapTool(tool)`。一个 Action 同时出现在菜单和工具栏，不需要复制成两个 Action；两处状态会同步。切回平移后，`QgsMapTool` 关联的 Action 和互斥组负责勾选变化。以后若要求“只有某类图层时可用”，再监听图层树的 `currentLayerChanged` 并在同一个 Action 上调用 `setEnabled()`，不要把第二个条件写成另一份按钮。

### 第 5 步：别漏对话框反过来调用宿主的地方

这个例子最容易卡在对话框内部按钮。打开复制后的 `src/app/qgsmeasuredialog.py`，搜索 `tool.mApp`。你会看到两处：

```python
tool.mApp.options('地图工具')
tool.mApp.mActionHelpContents.trigger()
```

这不是 QGIS 自动提供的 API，而是本项目给宿主约定的接口。范例的 `QgisApp.options()` 用一个小数位数对话框写入 `QgsSettings`，写完后通知四个测量工具刷新结果；`mActionHelpContents` 调用 QGIS 的帮助入口。自己的主窗口如果不叫 `QgisApp`，也没关系，只要传给 `QgsMeasureTool(..., app=self)` 的对象确实提供这些成员。若你原应用已经有“设置”和“帮助”窗口，可以在**复制后的** `qgsmeasuredialog.py` 将这两处回调改为自己已有的方法；不要在本仓库原文件里改，免得以后更新本项目时混在一起。

除了显式的 `tool.mApp`，对话框还通过 `QgsProject.instance()` 读工程椭球，通过画布 CRS 和 `QgsDistanceArea` 计算数值，通过 `QgsSettings` 读 `qgis/measure/decimalplaces`。因此“窗口能弹出来”还不足以验收测量：工程 CRS、单位、椭球和更新信号都要有实际值。

### 第 6 步：搬到你已经写好的主窗口

范例跑通以后，迁到自己的应用只需把上面的**位置**对应过去，不是把整个范例 `main.py` 复制进现有入口：

| 范例中的位置 | 你现有程序应该放在哪里 |
| --- | --- |
| `main()` 的 QGIS 初始化 | 你已有的唯一启动入口；只补缺少的 `initEditors()` 等初始化 |
| `mProject`、`mMapCanvas` | 主窗口构造时，其他地图工具创建之前 |
| `mMapTools` 五个工具 | 主窗口的地图工具初始化方法，或构造时画布创建之后 |
| `mActionMeasure...`、`mMapToolActionGroup` | 菜单/工具栏初始化方法；如果 `.ui` 已建同名 Action，就直接连接原 Action |
| `options()`、`mActionHelpContents` | 主窗口提供的设置、帮助入口；也可修改复制后的对话框回调 |
| `closeEvent()` 中关闭测量窗口 | 你原有的退出清理流程，必须早于 `exitQgis()` |

比如宿主用 Qt Designer 创建了 `mActionMeasure`：不要再执行范例中 `QAction(...)` 的那行。先 `uic.loadUi(...)` 得到 `self.mActionMeasure`，然后把现有 Action 加入组、调用 `self.mMapTools['measure'].setAction(self.mActionMeasure)`，再连接它的 `triggered`。这就是“迁移 Action 接线”和“再建一个同名 Action”的区别。

这个范例有意没有复制原项目的 `qgisapp.ui`、`qgisappinterface.py` 或整个 `images/` 目录。完成测量只需要列出的四个 `.py`、一个 `.ui`，以及宿主里可见的工程、画布和两个回调。遇到其他 Action 时，照本手册后面的“入口 → 类 → UI/资源 → 宿主对象 → 状态 → 清理”顺序继续查，不要把这里的文件名单当作所有 Action 的通用清单。

### 出错时先看这几项

| 看到的现象 | 先检查哪一处 |
| --- | --- |
| `No module named 'qgis'` | 是否通过 `python-qgis-ltr.bat` 启动，而不是直接用系统 Python |
| `No module named 'src.app.qgsmeasuretool'` | 四个 Python 文件是否在**范例项目**的 `src/app`，入口是否为范例项目的 `main.py` |
| 找不到 `qgsmeasurebase.ui` | 文件是否在 `src/ui`，有无把 `src/app` 改成平铺目录却没调整 `Path(__file__)` |
| 工具栏按钮亮着，画布点击无效 | `triggered` 是否调用了 `mMapCanvas.setMapTool()`；工具对象是否保存在 `self.mMapTools` |
| 点击“配置”时报 `options` 不存在 | 宿主缺 `options()`；看范例的同名方法或改复制后的对话框回调 |
| 点击“帮助”时报 `mActionHelpContents` 不存在 | 宿主缺帮助 Action；按范例建立，或将回调接到你的帮助入口 |
| 对话框数字不更新或单位不对 | 查工程 CRS/椭球、`mUnitsCombo`、`destinationCrsChanged` 信号与 `updateSettings()` |
| 退出时报 Qt/QGIS 对象错误 | 检查测量对话框和地图工具是否在 `QgsApplication.exitQgis()` 之前关闭 |

## 一、先选移植范围

| 想要的结果 | 建议取走的东西 | 典型例子 |
| --- | --- | --- |
| 只要某个操作 | 另一个应用自己建 `QAction`，照着本项目的方法调用 PyQGIS | 缩放至全图、刷新地图 |
| 要保留本项目的操作行为 | Action、处理方法，以及该方法实际引用的类 | 打开属性表、矢量编辑 |
| 要一组交互工具 | Action、`QgsMapTool` 子类、对话框和状态接线 | 测量、标注、形状数字化 |
| 要一个完整窗口或子系统 | 整个 `src/app` 子目录、对应的 `src/ui`、资源、宿主接口 | 地理配准、布局、网孔编辑 |
| 要接近本项目完整桌面 | `main.py`、`QgisApp`、界面、资源和依赖模块 | 以本项目为新应用的基底 |

只移植几个按钮时，第一、二种做法最省事。打算逐步搬走大量菜单、面板和工具栏时，保留本仓库的 `src/` 包结构更稳，避免到处改相对导入。

本项目的类名和文件名大体沿用 C++ 侧名称，例如 `QgisApp`、`QgsMapToolAddFeature`、`qgsmaptooladdfeature.py`。新宿主如果也要保持这种对应关系，建议保留 `mAction...` 的 `objectName`，但不必照搬整个 `QgisApp` 类。

## 二、先弄清项目如何启动

当前入口是仓库根目录的 `main.py`。它不是普通 Python 环境下直接运行的脚本：PyQGIS、Qt、GDAL 和 QGIS provider 都来自 OSGeo4W。先在本机确认 QGIS 3.34.10 对应的 OSGeo4W 安装，然后在 PowerShell 中运行：

```powershell
& 'C:\OSGeo4W\bin\python-qgis-ltr.bat' '%qgis_python%main.py'
```

路径要按自己的安装位置调整。如果是把代码移到另一个项目，运行的应是**那个项目**的入口；`python-qgis-ltr.bat` 仍负责提供 QGIS 环境。不要把这条命令理解为“移植后的应用必须依赖本项目的 `main.py`”。

`main.py` 中有几步顺序不能随便颠倒：确定 `QGIS_PREFIX_PATH` 与 profile，`QgsApplication.setPrefixPath(...)`，创建 `QgsApplication`，注册本项目资源，再初始化 QGIS 和构造主窗口。它还将 QGIS 自带的 `python/plugins` 加到 `sys.path`，初始化 GUI 编辑器部件，并在退出时先让主窗口清理工具与插件，再调用 `exitQgis()`。别的应用如果已有正确的 PyQGIS 启动流程，只需补齐被移植功能额外依赖的步骤，无须复制整个启动器。

本项目默认将配置写在自己的 `QGIS-Python/QGIS-Python-3.34` 应用名下。移植 Action 时检查用到的 `QgsSettings` 键；不同应用名可能对应不同的设置目录。需要复用原生 QGIS 配置时，本项目的入口支持 `QGIS_PYTHON_NATIVE_PROFILE=1`，但移植后的应用要自行决定是否采用这套策略。

## 三、一个 Action 在哪里

查一个 Action，按下面的顺序看，通常比通读 `qgisapp.py` 快。

1. 在 `src/ui/qgisapp.ui` 找 `<action name="mAction...">`：确认 `objectName`、文字、图标、快捷键，以及它被加入了哪个菜单或工具栏。
2. 在 `src/app/qgisapp.py` 搜同名对象：先看 `createActions()` 的绑定，再看 `createMapTools()`、`createToolBars()`、`updateActionState()`。部分操作是动态创建的，主 `.ui` 中找不到。
3. 跟到实际处理方法。普通操作可能直接调用 `QgsProject`、`QgsMapCanvas` 或 `QgsVectorLayer`；复杂操作会实例化 `src/app/` 中的类。
4. 继续查处理方法的导入、构造参数、`self.m...` 成员、加载的 `.ui`、图标路径和关闭时的清理。

在仓库根目录可以这样查，例如查测量和地理配准：

```powershell
rg -n 'mActionMeasure|mActionShowGeoreferencer' src/ui/qgisapp.ui src/app
rg -n 'def createMapTools|def createActions|def createToolBars|def updateActionState' src/app/qgisapp.py
rg -n 'from images|:/images|uic.loadUi|ui_' src/app/qgsmeasuretool.py src/app/georeferencer
```

`rg` 找到的 `bind('mActionX', ...)` 是入口，不一定是实现的全部。`createToolBars()` 还会把某些现成 Action 收进 `QToolButton` 的弹出菜单，保存默认选项；右键菜单可能由菜单提供器在运行时组装，插件也会在启动后动态添 Action。

本项目的 `bind(name, callback, requirement=None, note='', signal='triggered')` 按名字找到 Action，连接 `triggered` 或 `toggled`，并记录启用条件。它适合本项目自己的窗口，**不是可以单独复制到另一个窗口就自动工作的框架**。移植时可以自己连接信号，也可以连同状态管理一起移植。

## 四、给另一个主窗口接上 Action

以下是最小例子：宿主已有 `QgsMapCanvas`，想加入“缩放至全图”。它用的是 QGIS 自带的地图画布方法，不需要复制本项目的 `.py` 文件。

```python
from qgis.PyQt.QtWidgets import QAction, QToolBar


class MyWindow(...):
    def addZoomFullExtentAction(self):
        self.mActionZoomFullExtent = QAction('缩放至全图', self)
        self.mActionZoomFullExtent.setObjectName('mActionZoomFullExtent')
        self.mActionZoomFullExtent.triggered.connect(
            self.mMapCanvas.zoomToFullExtent
        )

        toolbar = QToolBar('地图导航', self)
        self.addToolBar(toolbar)
        toolbar.addAction(self.mActionZoomFullExtent)
```

示例中的 `...` 表示宿主已有自己的 `QMainWindow` 基类和构造过程；`self.mMapCanvas` 必须先创建。这段展示的是 Action 的接法，不是新的完整启动脚本。若要保留本项目额外的缩放行为，就查 `QgisApp.zoomFull()`，按需要搬走其逻辑。

若宿主用 Qt Designer，可以在宿主 `.ui` 中创建同名 Action，让 `uic.loadUi()` 或生成的 `Ui_...` 类提供 `self.mActionZoomFullExtent`，然后只写 `triggered.connect(...)`。不要同时在 `.ui` 和 Python 中各建一份同名按钮。

对任何 Action，都按同样的次序接：

1. **先准备宿主对象。** 常见的有 `mProject = QgsProject.instance()`、`mMapCanvas`、当前图层获取方法、`mMessageBar`、状态设置和停靠面板。
2. **再建功能对象。** 地图工具、对话框、模型和 provider 必须先于信号绑定准备好；惰性创建的窗口可以在第一次触发时创建。
3. **最后建 Action 并连接。** 注意 `triggered` 与 `toggled` 的区别。可选工具要 `setCheckable(True)`，互斥地图工具通常放入 `QActionGroup`。
4. **同步状态。** 图层切换、选择变化、编辑模式变化后，重新算 `setEnabled()`、`setChecked()`；退出时断开外部插件或工具占用的资源。

`updateActionState()` 中的 `layer`、`vector`、`editing`、`raster`、`capture-technique`、`geometry-edit` 等要求只是本项目的分类，实际还有 provider 能力、几何类型、选择集、撤销状态等判断。不要把 `setEnabled(True)` 当作移植完成。

### Action 的“开关状态”由谁管

这里有三种容易混在一起的状态：

| 属性 | 表示什么 | 例子 |
| --- | --- | --- |
| `setEnabled()` | 当前条件下能不能执行；灰色通常是 `False` | 没有可编辑矢量图层时，禁用“新增要素” |
| `setChecked()` | 可勾选 Action 目前是不是选中的模式 | 当前处于平移工具、编辑模式或已开启捕捉 |
| `setVisible()` | 菜单或工具栏里要不要显示这个入口 | 某些工具栏项目按插件或界面配置隐藏 |

Qt 的 `QAction` 保存这些状态，`QActionGroup` 管一组互斥选择。PyQGIS 提供图层、工程、provider、画布和地图工具的状态与信号；它没有一个“把任意新 Action 注册进去，就自动知道什么时候该灰、该勾”的通用管理器。原版 QGIS 3.34.10 的 `QgisApp::activateDeactivateLayerRelatedActions()` 集中处理大量图层相关操作，`mMapToolGroup` 负责地图工具互斥。本项目对应的集中入口是 `src/app/qgisapp.py` 中的 `updateActionState()`；另有 `QgsSnappingWidget`、`QgsMapToolsDigitizingTechniqueManager`、网孔编辑工具等负责自己的状态。移植某个 Action 时，要找到**实际管理它的那段代码**，一并搬走或在新宿主中重写。

本项目的 `bind(..., requirement='editing')` 只是把 Action 记到 `mRequirements`，随后由 `updateActionState()` 按当前图层判断；`bind()` 自身并不会持续监听图层。`createActions()` 创建工具与绑定后，主窗口还会在初始化末尾刷新一次。若只复制 `bind()`，没接状态变化信号，第一次图层切换以后按钮就可能一直保持旧状态。

状态要从真正的数据来源读，不能只读按钮自己。下面是几个常见对应关系：

| Action 类别 | 状态来源 | 需要监听的变化 |
| --- | --- | --- |
| 图层操作 | 当前图层类型、有效性 | 图层树当前图层变化、图层增删 |
| 矢量编辑 | `layer.isEditable()`、`layer.isModified()`、provider 能力 | 开始/停止编辑、图层修改、图层切换 |
| 选择集操作 | `selectedFeatureCount()`、几何类型 | 要素选择变化、图层切换 |
| 撤销/重做 | 当前图层的 `undoStack().canUndo()/canRedo()` | 撤销栈变化、图层切换 |
| 地图工具 | `mMapCanvas.mapTool()` | `mapToolSet`；工具激活、停用 |
| 捕捉、拓扑编辑 | `QgsProject.snappingConfig()`、工程拓扑设置 | 工程配置变化；本项目见 `qgssnappingwidget.py` |
| 停靠面板 | Dock 实际可见状态 | Dock 的 `visibilityChanged`；可用 `toggleViewAction()` |
| 插件提供的 Action | 插件自己及其所需数据源 | 插件装载/卸载、provider 可用性和插件自身信号 |

新宿主只需管理自己移植的 Action。下面的例子假定主窗口已经有 `mProject`、`mMapCanvas`、`mLayerTreeView`，以及从 `.ui` 或 Python 创建的 `mActionToggleEditing`、`mActionAddFeature`、`mActionDeleteSelected`、`mActionUndo`、`mActionRedo`。它展示状态接线，编辑动作本身仍需连接你自己的 `toggleEditing()` 等处理方法。

```python
from qgis.core import QgsVectorLayer, QgsVectorDataProvider


def connectActionState(self):
    self.mLayerTreeView.currentLayerChanged.connect(self.updateActionState)
    self.mProject.layersAdded.connect(self.connectLayerStateSignals)
    self.mProject.layersRemoved.connect(self.updateActionState)
    self.mMapCanvas.mapToolSet.connect(self.updateActionState)
    self.connectLayerStateSignals(list(self.mProject.mapLayers().values()))
    self.updateActionState()


def connectLayerStateSignals(self, layers):
    for layer in layers:
        if not isinstance(layer, QgsVectorLayer):
            continue
        layer.editingStarted.connect(self.updateActionState)
        layer.editingStopped.connect(self.updateActionState)
        layer.layerModified.connect(self.updateActionState)
        layer.selectionChanged.connect(self.updateActionState)
        layer.undoStack().canUndoChanged.connect(self.updateActionState)
        layer.undoStack().canRedoChanged.connect(self.updateActionState)
    self.updateActionState()


def updateActionState(self, *args):
    layer = self.mLayerTreeView.currentLayer()
    vector = layer if isinstance(layer, QgsVectorLayer) else None
    editable = bool(vector and vector.isEditable())
    caps = vector.dataProvider().capabilities() if vector else 0
    selected = vector.selectedFeatureCount() if vector else 0

    self.mActionToggleEditing.setEnabled(bool(
        vector and (editable or
                    (not vector.readOnly() and
                     caps & QgsVectorDataProvider.EditingCapabilities))
    ))
    self.mActionAddFeature.setEnabled(bool(
        editable and caps & QgsVectorDataProvider.AddFeatures
    ))
    self.mActionDeleteSelected.setEnabled(bool(
        editable and selected > 0 and
        caps & QgsVectorDataProvider.DeleteFeatures
    ))
    self.mActionUndo.setEnabled(bool(vector and vector.undoStack().canUndo()))
    self.mActionRedo.setEnabled(bool(vector and vector.undoStack().canRedo()))

    action = self.mActionToggleEditing
    old_blocked = action.blockSignals(True)
    action.setChecked(editable)
    action.blockSignals(old_blocked)
```

把这三个方法放入主窗口类，然后在相关 Action 和图层树都创建完以后调用一次 `connectActionState()`。示例对已在工程中的图层和后来加入的图层各连接一次；若宿主自己还会反复调用连接方法，应加上去重。`*args` 是为了兼容这些 Qt 信号各自不同的参数。

这里先更新 `enabled`，再把“切换编辑”的勾选状态同步到 `layer.isEditable()`。同步勾选时临时阻断该 Action 的信号，是为了避免程序更新按钮外观时又触发自己的 `toggled` 处理器。真正执行“开始/停止编辑”的回调必须另外接，而且要处理保存、放弃及失败的情况；`setChecked(True)` 并不会让图层进入编辑模式。原版与本项目还会在当前图层失去编辑条件时切回平移工具，避免画布留在已经不能用的数字化工具上。

地图工具的勾选状态应由**当前画布工具**反映：用 `tool.setAction(action)` 关联工具，工具 Action 加入互斥的 `QActionGroup`；需要判断可用性时仍由宿主刷新 `setEnabled()`。捕捉开关则由 `QgsProject.snappingConfig()` 决定，点击 Action 要先修改工程配置，工程配置变化后再同步按钮。本项目 `QgsSnappingWidget.projectSnapSettingsChanged()` 就这样做。Dock 最简单的接法是直接把 `dock.toggleViewAction()` 放进“视图 → 面板”菜单；若必须保留独立的 `mAction...`，将它的 `toggled` 连到 Dock 的可见性，并将 Dock 的 `visibilityChanged` 连回 Action，保证用窗口关闭按钮收起 Dock 后勾选也会取消。

### 地图工具有额外的一步

`QgsMapTool` 不是点一次就执行完的普通回调。Action 触发后要切换画布工具；换到平移、缩放或其他工具时，也要让旧工具退出。参考 `QgisApp.createMapTools()` 和 `setMapTool()`：本项目把工具保存在 `self.mMapTools`，把工具 Action 放入互斥组，再用 `tool.setAction(action)` 让工具状态与按钮对应。宿主若只在点击时实例化局部变量 `tool`，可能因为 Python 对象被回收而出现“按钮能点、画布没反应”。把工具保存在窗口或长期存活的控制器上。

### 对话框与停靠窗也需要宿主管理

模态对话框可以 `exec_()`；非模态窗口要由宿主保存引用。属性表的实现把创建的 `QgsAttributeTableDialog` 放进 `self.mWindows` 后 `show()`。想复制原有的停靠切换行为，还要看该对话框如何从窗口变成 Dock，以及主窗口的 `addDockWidget()`、状态保存与恢复。Python 控制台同理：`src/app/qgspythonconsole.py` 和 `src/gui/qgsdockablewidgethelper.py` 管的是宿主与停靠行为，不能只复制控制台里面那层 widget。

### 图层树、浏览器及其他面板怎样搬

之前的依赖表只回答了“去哪里找”，还不足以指导面板移植。面板通常由**内容 widget、宿主 Dock、模型、工程/画布连接、面板工具栏、菜单操作**六部分组成；少一部分可能仍能显示，但行为不会像本项目。先确认新应用已经有 `QMainWindow`、`mProject = QgsProject.instance()`、`mMapCanvas` 和一个 `mPanelMenu`。接下来按“先图层树，再浏览器，再依赖图层树的其他面板”的顺序处理。

**图层树。** `QgisApp.createLayerTreeView()` 使用的是 QGIS 原生的 `QgsLayerTreeView`、`QgsLayerTreeModel` 和 `QgsLayerTreeMapCanvasBridge`。模型指向 `mProject.layerTreeRoot()`，bridge 把树上的顺序、可见性与画布连接起来。工具栏和 `QgsAppLayerTreeViewMenuProvider` 是本项目加在原生控件外面的；在 `src/ui/qgisapp.ui` 里找不到这部分按钮的完整接线。

以下是只搭起“树、模型、画布联动、可停靠面板”的骨架。示例方法放在新应用的 `QMainWindow` 类里，调用前须已创建画布和工程：

```python
from qgis.PyQt.QtCore import Qt
from qgis.PyQt.QtWidgets import QDockWidget
from qgis.core import QgsLayerTreeModel
from qgis.gui import QgsLayerTreeView, QgsLayerTreeMapCanvasBridge


def createLayerTreeView(self):
    root = self.mProject.layerTreeRoot()
    self.mLayerTreeView = QgsLayerTreeView(self)
    self.mLayerTreeModel = QgsLayerTreeModel(root, self)
    for flag in (QgsLayerTreeModel.AllowNodeRename,
                 QgsLayerTreeModel.AllowNodeReorder,
                 QgsLayerTreeModel.AllowNodeChangeVisibility,
                 QgsLayerTreeModel.ShowLegendAsTree):
        self.mLayerTreeModel.setFlag(flag)
    self.mLayerTreeView.setModel(self.mLayerTreeModel)
    self.mLayerTreeCanvasBridge = QgsLayerTreeMapCanvasBridge(
        root, self.mMapCanvas, self
    )

    self.mLayerTreeDock = QDockWidget('图层', self)
    self.mLayerTreeDock.setObjectName('Layers')
    self.mLayerTreeDock.setWidget(self.mLayerTreeView)
    self.addDockWidget(Qt.LeftDockWidgetArea, self.mLayerTreeDock)
    self.mPanelMenu.addAction(self.mLayerTreeDock.toggleViewAction())
    self.mLayerTreeView.currentLayerChanged.connect(self.activateLayer)
```

这段只完成骨架；`activateLayer(layer)` 仍要由宿主实现，至少更新画布当前图层及与它相关的属性、编辑、撤销 Action。要复制本项目图层面板的外观和操作，再按 `createLayerTreeView()` 增加顶部工具栏：添加组、地图主题、图例过滤、展开/折叠、移除图层、样式面板。其中地图主题来自 `src/app/qgsmapthemes.py`；表达式过滤调用 `toggleFilterLegendByExpression()` 和 `updateFilterLegend()`；样式面板通过 `mapStyleDock()` 惰性创建。不要用 `setWidget(self.mLayerTreeView)` 后再把工具栏也塞给同一个 Dock；本项目创建了容器和垂直布局，把工具栏与树一起放进容器。

要原样搬图层右键菜单，取 `src/app/qgsapplayertreeviewmenuprovider.py`，并调用 `mLayerTreeView.setMenuProvider(...)`。但这个类的构造参数包含 `app`，内部直接访问 `app.removeLayer`、`app.saveAsLayerDefinition`、`app.mActionOpenTable`、`app.mActionLayerSaveAs`、`app.customLayerActions` 等入口。新宿主必须实现所用入口，或在该类中删去/改接不打算支持的菜单项。QGIS 自带的 `view.defaultActions()` 可以提供一些基础树操作，却不会替你实现本项目那组“属性表、样式、导出、插件图层 Action”的右键菜单。

**浏览器。** 本项目 `createDockWidgets()` 先建立 `QgsBrowserGuiModel` 并调用 `initialize()`，再用该模型构造 QGIS 原生的 `QgsBrowserDockWidget`。它本身就是 Dock，不要再包一层 `QDockWidget`。最小接法如下：

```python
from qgis.PyQt.QtCore import Qt
from qgis.gui import QgsBrowserGuiModel, QgsBrowserDockWidget


def createBrowserDock(self):
    self.mBrowserModel = QgsBrowserGuiModel(self)
    self.mBrowserModel.initialize()
    self.mBrowserWidget = QgsBrowserDockWidget(
        '浏览器', self.mBrowserModel, self
    )
    self.mBrowserWidget.setObjectName('Browser')
    self.mBrowserWidget.setDisabledDataItemsKeys(['gpx'])
    self.mBrowserWidget.setMessageBar(self.mMessageBar)
    self.mBrowserWidget.openFile.connect(
        lambda path, hint='': self.openFile(path)
    )
    self.mBrowserWidget.handleDropUriList.connect(self.handleDropUriList)
    self.addDockWidget(Qt.LeftDockWidgetArea, self.mBrowserWidget)
    self.mPanelMenu.addAction(self.mBrowserWidget.toggleViewAction())
```

这里的宿主方法是 `openFile(path)` 和 `handleDropUriList(uris)`；浏览器的 `openFile` 信号可能附带第二个 `hint` 参数，所以示例用 lambda 只把路径转给宿主。本项目对应方法在 `qgisapp.py`。只显示浏览器而不接它们，点某些项目或拖入数据时可能没有结果。浏览器能看到哪些数据源，还取决于 OSGeo4W 中安装、启用的 provider。本项目调用 `setDisabledDataItemsKeys(['gpx'])` 排除了 GPX/GPS 数据项；移植时要继续遵守“排除 GPS”的要求，就保留这一步，并留意插件是否重新添了相关入口。

**其他面板的真实构成。** 以下按当前仓库代码列，不把“有一个 Dock”说成“完整复制了 QGIS 原版”：

| 面板 | 当前实现与文件起点 | 移植时不能漏掉的连接 |
| --- | --- | --- |
| 图层 | 原生树/模型/画布 bridge；`createLayerTreeView()`，自写菜单提供器、工具栏 | 当前图层、画布、右键回调、地图主题、图例过滤、状态刷新 |
| 浏览器 | 原生 `QgsBrowserGuiModel` + `QgsBrowserDockWidget`；`createDockWidgets()` | 模型初始化、文件打开、URI 拖入、消息栏、provider |
| 高级数字化 | 原生 `QgsAdvancedDigitizingDockWidget` | 先于新增要素及几何地图工具创建；传给这些工具 |
| 撤销/重做 | Qt `QUndoView` 包在 Dock 中 | 当前图层变化时切换到该图层的 `undoStack()`；图层移除时清空 |
| 日志消息 | 原生 `QgsMessageLogViewer` 包在 Dock 中 | `QgsApplication.messageLog()`、状态栏消息按钮与 Dock 可见性互相同步；日志标签由发消息的模块决定 |
| 时间控制器 | 原生 `QgsTemporalControllerWidget` | 将它的 `temporalController()` 设给地图画布 |
| 识别结果 | `src/ui/qgsidentifyresultsbase.ui`、`src/app/qgsidentifyresultsdialog.py`、`src/app/qgsidentifyplot.py`、`src/app/qgsmaptoolidentifyaction.py` | 树/表/图表、栅格 HTML/Text 查看与打印、五种地图选择方式、复制/缩放/表单/高亮、Dock |
| 图层顺序 | 原生 `QgsCustomLayerOrderWidget` | 使用同一个图层树—画布 bridge |
| 概览 | 原生 `QgsMapOverviewCanvas` | 绑定主画布，并交给 `mLayerTreeCanvasBridge.setOverviewCanvas()` |
| 空间书签 | 原生 `QgsBookmarkManagerModel` + Qt `QTableView` | 同时传应用书签管理器和工程书签管理器；双击跳转 |
| 顶点编辑器 | `src/app/vertextool/qgsvertexeditor.py` | 地图画布、当前顶点工具、选中顶点与编辑图层 |
| 统计摘要 | `src/app/qgsstatisticalsummarydockwidget.py` | 画布、当前图层、后台任务；退出前 `shutdown()` |
| 图层样式 | `src/app/qgslayerstylingwidget.py`，由 `mapStyleDock()` 按需创建 | 当前图层、地图画布、样式改变后刷新图例；Dock 可见性与 Action 勾选同步 |
| 处理工具箱 | OSGeo4W `processing.ProcessingPlugin` 创建，`initProcessing()` 接入 | 可工作的 `QgisInterface`、处理注册表、插件工具栏/Dock 方法 |
| Python 控制台 | `src/app/qgspythonconsole.py` + `src/gui/qgsdockablewidgethelper.py` | `iface`、Dock 的创建及恢复、菜单勾选和退出保存 |
| 高程剖面 | `src/app/elevation/` 中的 `QgsElevationProfileWidget` 按需创建 | 画布、图层、导出 UI，退出前关闭其任务和工具 |
| 属性表 | `src/app/qgsattributetabledialog.py` 创建的独立窗口，可切换停靠 | 当前矢量图层、过滤模式、窗口引用和 Dock 状态 |

识别结果面板使用原版 `qgsidentifyresultsbase.ui` 的布局和 Action 名称。由于 OSGeo4W 的 Python 绑定没有暴露 `QgsIdentifyResultsDialog` 和 Qwt，图表由 `qgsidentifyplot.py` 的 Qt 绘制控件实现：它和原版一样按键排序、跳过非数值，给每层绘制曲线与图例。栅格 HTML/Text 结果显示在树内；选中结果后可用 Qt 打印对话框打印。栅格格式下拉框会保存所选格式并在原识别位置重查。识别工具的五种形状调用 PyQGIS 已绑定的坐标及几何识别接口。要素树现在包含动作、按字段配置格式化后的属性，以及关联图层的结果；右键菜单接入了图层注册 Action 和字段 Action。移植时把上表列出的四个文件与宿主 `mQgisInterface`、`mPanelMenu`、`mMapCanvas` 一起复制，并保证 OSGeo4W 的 QtPrintSupport 可用。这里实现的是面板行为，不是把 QGIS 的 C++ 类或 Qwt 二进制组件直接绑定到 Python；Qwt 的绘图外观和原版特定 HTML 引擎的细节仍有差异。

**停靠布局与恢复。** 本项目的普通面板通过 `dock(name, title, widget, area, visible)` 创建：设置稳定的 `objectName`，加入 `QMainWindow`，再把 `toggleViewAction()` 加到“视图 → 面板”。新宿主也应给每个 Dock、工具栏一个稳定且唯一的 `objectName`；`saveState()`/`restoreState()` 是靠它们认位置的。当前项目在首次 `showEvent()` 中恢复 `UI/state`，关闭时保存 `UI/state` 和 `UI/geometry`。恢复之前要创建需要恢复的 Dock；按需创建的 Python 控制台如果上次是打开状态，就由 `restorePythonConsole()` 先重建，再调用 `restoreState()`。照搬设置键也要注意新应用的 `QgsSettings` 应用名，否则可能读到本项目旧布局。

复制多个面板时，先分别验证内容是否可用，再检查“视图 → 面板”开关、拖动停靠、浮动、关闭重开和布局恢复。不能仅凭 Dock 出现在屏幕上判断面板移植完成。

## 五、常见功能该取哪些文件

下表给的是**查依赖的起点**，不是承诺“仅复制这些文件就能运行”。表里写“整个目录”时，还要检查该目录导入的同级模块和对应 `.ui`。

| 功能 / Action | 实现起点 | 宿主需要提供什么 |
| --- | --- | --- |
| 导航、缩放、刷新 | `src/app/qgisapp.py` 的 `createActions()`、`createMapTools()` | `QgsMapCanvas`、工程和地图工具生命周期 |
| 图层树、图层右键菜单 | `createLayerTreeView()`；`src/app/qgsapplayertreeviewmenuprovider.py` | 图层树模型、当前图层、工程与菜单回调 |
| 浏览器 | `createDockWidgets()` 及 QGIS 自带的浏览器模型和 Dock | QGIS 数据源 provider、宿主 Dock 和图层加载入口 |
| 属性表及筛选、停靠 | `QgisApp.attributeTable()`；`src/app/qgsattributetabledialog.py` | 当前矢量图层、窗口引用、停靠管理、编辑状态 |
| 新增要素、几何编辑 | `src/app/qgsmaptooladdfeature.py`、`src/app/qgsmaptoolgeometryedit.py` 与相关 `qgsmaptool*.py` | `mAdvancedDigitizingDockWidget`、`mVectorLayerTools`、捕捉、撤销、可编辑图层 |
| 高级数字化、捕捉 | `createMapTools()`、`createToolBars()`、`src/app/qgssnappingwidget.py` | CAD Dock、画布捕捉工具、工程捕捉配置、几何工具 |
| 距离与面积测量 | `src/app/qgsmeasuretool.py`、`src/app/qgsmeasuredialog.py`、`src/ui/qgsmeasurebase.ui` | 画布、测量单位与工程 CRS、状态更新 |
| 角度与方位测量 | `src/app/qgsmaptoolmeasureangle.py`、`src/app/qgsmaptoolmeasurebearing.py` | 画布、对应 Action 和地图工具切换 |
| 标注工具 | `src/app/labeling/`、`src/ui/labeling/` | 当前可编辑矢量图层、渲染结果、辅助存储、CAD Dock |
| 注记工具 | `src/app/qgsmaptool*annotation.py`、相关注记对话框和 `.ui` | 注记图层、画布、QGIS 注记类型注册表 |
| 形状数字化 | `src/gui/maptools/`、`createToolBars()` 的相关组装 | CAD Dock、捕捉、编辑图层与工具组 |
| 栅格计算器 | `src/app/qgsrastercalcdialog.py`、`src/ui/qgsrastercalcdialogbase.ui` | 栅格图层、工程、输出路径和窗口宿主 |
| 地理配准 | `src/app/georeferencer/`、`src/ui/georeferencer/` | 主画布、工程、浏览器模型、图层加载、GDAL/变换环境 |
| DWG 导入 | `src/app/dwg/`、`src/ui/qgsdwgimportbase.ui` | 对应 GDAL/CAD 能力、输出数据源与图层加载 |
| 网孔编辑与计算 | `src/app/mesh/`、`src/ui/mesh/` | 网孔图层、编辑工具、撤销与关闭前清理 |
| 布局与报告 | `src/app/layout/`、`src/ui/layout/` | 工程布局管理、布局设计器和 Dock/窗口宿主 |
| 高程剖面 | `src/app/elevation/`、`src/ui/qgselevationprofile*.ui` | 画布、图层、工具与导出设置 |
| 装饰项 | `src/app/decorations/`、`src/ui/qgsdecoration*.ui` | 画布、工程、设置和绘制生命周期 |
| Python 控制台 | `src/app/qgspythonconsole.py`、`src/gui/qgsdockablewidgethelper.py` | QGIS Python console 组件、`iface`、Dock 和状态保存 |
| 处理工具箱、核心插件 | `initProcessing()`、`initCorePlugins()`、`src/app/qgisappinterface.py` | 处理框架、插件搜索路径、较完整的 `QgisInterface` 实现 |
| 状态栏、任务与日志 | `createStatusBar()`、`src/app/qgsstatusbar*widget.py`、`src/gui/qgstaskmanagerwidget.py` | 画布信号、工程、消息日志 Dock、任务管理器 |

有些功能直接使用 OSGeo4W 安装中的 `qgis.core`、`qgis.gui` 或 `python/plugins`，并没有一个必须复制的本项目模块。表中“宿主需要提供”是迁移时最容易漏掉的部分。GPS 功能不在本项目的移植目标内；不要因复制整个菜单或图标目录，就把 GPS 项目当成已实现功能。

## 六、两个不同类型的移植例子

### 例 1：移植“打开属性表”

先查 `src/ui/qgisapp.ui` 的 `mActionOpenTable`、`mActionOpenTableSelected`、`mActionOpenTableVisible` 和 `mActionOpenTableEdited`。它们不是四套属性表，而是同一个窗口的不同过滤模式。入口在 `QgisApp.attributeTable()`，窗口类在 `src/app/qgsattributetabledialog.py`。本项目调用前先用 `vectorLayer(...)` 取得有效矢量图层；打开后把窗口加到 `mWindows`，保持非模态窗口存活。

复制时，先把 `qgsattributetabledialog.py` 引用的本项目模块和它运行时加载的 `.ui` 找齐，再决定是否真的需要本项目的复杂属性表。如果新应用只要一张简单表，也可直接使用当前 QGIS 版本提供的原生表格组件，自己写宿主逻辑。若要保留本项目的“停靠/浮动切换、筛选、编辑、工具栏”，就必须移植窗口类及其宿主接口，不能只调一个 `show()`。

### 例 2：移植“地理配准”

`mActionShowGeoreferencer` 由 `QgisApp.showGeoreferencer()` 打开独立窗口。主体在 `src/app/georeferencer/`，界面在 `src/ui/georeferencer/`。目录里除主窗口还有 GCP 列表、画布工具、变换设置和图像变换逻辑。主窗口会访问宿主画布、工程、浏览器模型和加载图层的方法，因此推荐按整个子系统迁移，再为新主窗口实现它实际调用的接口。只复制 `qgsgeorefmainwindow.py`，很快会在导入、构造或点击内部按钮时碰到缺依赖。

迁移完成后逐个试窗口内部操作：载入栅格、添加/移动/删除控制点、选择变换、输出栅格、关闭再打开。主菜单 Action 只负责打开窗口，验证“能打开”还不能说明地理配准已移植。

## 七、`iface`、插件和处理工具箱

普通 PyQGIS 应用可以没有 `qgis.utils.iface`；很多 QGIS 插件却会通过它取得主窗口、当前图层、画布、菜单和工具栏。本项目在 `src/app/qgisappinterface.py` 定义 `QgisAppInterface(QgisInterface)`，主窗口创建它后赋给 `qgis.utils.iface`。`initProcessing()` 和 `initCorePlugins()` 使用 OSGeo4W 自带的插件目录启动处理框架及核心插件。数据库、矢量、Web 工具栏中的某些 Action 是插件运行时添加的，未必出现在 `qgisapp.ui`。

移植单个自有 Action 时通常不需要把本项目的 `iface` 搬走。要移植“由 QGIS 插件贡献 Action”的整组行为，就先列出插件会调用哪些 `QgisInterface` 方法；新宿主至少实现这些方法，确保 `qgis.utils.iface` 在插件初始化前已设置，并在退出时调用插件清理。不要仅仅把 `qgis.utils.iface` 指向普通 `QMainWindow`：插件访问 `mapCanvas()`、`activeLayer()`、`addPluginToMenu()`、`addVectorToolBarIcon()` 等方法时会失败。

处理工具箱还涉及算法 provider 注册、插件路径、任务反馈和处理结果加载。若目标应用只需要执行某个算法，直接调用处理框架通常比搬整套工具箱面板合适；若要原版工具箱交互，再检查本项目的初始化、Dock 和 `iface` 契约。

## 八、`.ui`、图标和资源怎么带走

本项目的主界面是 `src/ui/qgisapp.ui`，很多子窗口也在 `src/ui/`。多数 `.ui` 是代码运行时通过 `uic.loadUi(...)` 加载的，**不需要逐个 `pyuic` 编译**。仓库里真正提供批处理的只有 `scripts/build-ui.cmd`，它生成：

```text
src/ui/qgisapp.ui                  → src/ui/ui_qgisapp.py
src/ui/qgsrastercalcdialogbase.ui → src/ui/ui_qgsrastercalcdialogbase.py
```

`QgisApp` 会优先尝试生成的主界面模块，缺少时回退到动态加载 `.ui`。如果只拿走某个对话框，先看其 Python 类究竟是 `uic.loadUi`，还是导入 `ui_...`；按它使用的方式保留 `.ui` 或编译产物。Qt Designer 中的 QGIS 自定义 widget 还要求 `qgis.gui` 和相应 Python 类可导入，仅编译界面文件不够。

本项目自带图标集中在 `images/`，由 `images/images.qrc` 组织成 `:/images/...` 资源。若复制的 `.ui` 或 Python 代码用了这种路径，就带走需要的图片和 `.qrc` 条目，并在新应用创建界面前导入编译后的资源模块。编译命令在 `scripts/build-resources.cmd`：

```powershell
& 'C:\OSGeo4W\bin\python-qgis-ltr.bat' -m PyQt5.pyrcc_main .\images\images.qrc -o .\images\images_rc.py
```

生成的 `images/images_rc.py` 是构建产物，不在仓库中；新的应用如果换了包名，导入路径也要改。本项目 `main.py` 在构造窗口之前执行 `from images import images_rc`。若只用 `QgsApplication.getThemeIcon(...)` 提供的 QGIS 自带主题图标，不必为了它复制整套 `images/`。图标显示为空时先看 `QIcon(':/images/...').isNull()`，再查资源是否编译、是否导入、资源前缀是否一致。直接复制 SVG 文件但继续使用 `:/images/...` 路径，Qt 不会自动找到磁盘文件。

如保留 `src.app.xxx` 导入，建议新项目也保留 `src/` 在项目根目录下，并确保入口把根目录加入 `sys.path`。若改包结构，除了 import，还要查 `Path(__file__)` 推算根目录的代码和相对于根目录拼接的 UI、图片路径。例如地理配准模块就会向上查找项目资源；文件单独挪走后，这种路径常会指错地方。

## 九、状态、信号和生命周期检查

移植 Action 后，至少核对下面几项。它们也是“按钮可以点击，但没有效果”的常见原因。

| 现象 | 先查哪里 |
| --- | --- |
| 按钮出现但点击无反应 | `triggered` / `toggled` 接错；回调抛异常；地图工具未设为画布当前工具 |
| 点击后马上恢复或窗口消失 | 地图工具、非模态对话框或 Dock 没有被宿主保存引用 |
| Action 永远灰色或不该亮时亮 | 未移植 `updateActionState()` 的条件；当前图层类型、几何类型或 provider 能力不满足 |
| 编辑、捕捉工具启动却不能操作 | 图层未进入编辑、缺 CAD Dock、捕捉工具或矢量图层工具接口 |
| 图标为空 | `.qrc` 没编译/导入、`:/images/` 路径不匹配，或主题图标路径用错 |
| 对话框显示了却少控件 | 只复制了 `.ui`，漏掉 Python 里动态添加的按钮、模型和信号 |
| 插件菜单、数据库或 Web Action 缺失 | 插件未初始化、`iface` 方法不足、工具栏在插件加载前没有准备好 |
| 第二次打开出错或退出崩溃 | 窗口持有已销毁的图层/画布，或清理顺序晚于 `QgsApplication.exitQgis()` |

建议用一个内存图层和一个小栅格做手工验证：首次打开、切换当前图层、进入/退出编辑、切换工具、关闭并重开对话框、保存工程、退出应用。测量和标注还要在不同 CRS、不同图层上试；属性表要检查筛选、选中要素和停靠切换。这里只是验证建议，不要求为移植先建一整套测试框架。

## 十、实际搬运顺序

拿 `mActionX` 做一遍，后续 Action 按同一套顺序处理：

1. 用 `rg` 定位 `.ui`、`createActions()`/`createMapTools()`、真正的处理方法、状态条件和工具栏组装。
2. 写下处理方法访问的 `self.m...`、调用的宿主方法、导入的本项目模块，以及加载的 `.ui`、`:/images` 资源。再从新模块的构造函数、按钮回调往下追一层；有些依赖只有点开子窗口中的按钮才会触发。
3. 在新应用准备好画布、工程、图层树、消息栏等必须的宿主对象；不需要的本项目功能不搬。
4. 保留原类名、文件名和 `mAction...` 名称，把选中的模块及其实际依赖复制到新项目；调整 import 和路径。
5. 连接 Action 与回调或地图工具，再接图层变化、编辑状态、选择变化和启用/选中状态。
6. 只编译真正被使用的 `.ui` 与 `.qrc`，其余可保持运行时加载；检查图标、Dock 和快捷键是否重复。
7. 手工走通完整操作与关闭重开流程，然后再搬下一个 Action。

第 2 步可以记成一张小表：`Action 名称 → 处理方法 → Python 文件 → UI/资源 → 宿主对象 → 启用条件 → 关闭清理`。这比按工具栏批量复制更容易发现缺口。

## 十一、代码使用与来源

本仓库的许可证见根目录 `LICENSE`，内容为 GPLv3。移植本项目代码时要保留相应的版权和许可证信息。QGIS、OSGeo4W 及其附带插件另有各自的许可与发布要求；打包、再发布前按实际带走的代码和依赖核对。文中列出的文件是本仓库的实现位置，不能替代对最终发布包的许可检查。
